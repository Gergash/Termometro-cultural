"""Report generation (summaries, exports). Placeholder."""
from typing import Any, Dict, List


def build_summary_report(aggregates: List[Dict[str, Any]]) -> Dict[str, Any]:
    """Build a summary report from aggregation results."""
    return {"generated_at": None, "aggregates": aggregates}
